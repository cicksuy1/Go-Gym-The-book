//go:build !solution

package arrays

// Sum adds up all the numbers in a slice and returns the total.
//
// MODULE 3 — ARRAYS & SLICES.  Make the test pass by ranging over the slice.
// Right now it returns the wrong thing (that's the RED state).
func Sum(numbers []int) int {
	return 0 // TODO(you): range over numbers and add them up
}

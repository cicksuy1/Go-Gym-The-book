//go:build !solution

package integers

// Add returns the sum of two integers.
//
// MODULE 1 — INTEGERS.  Your job: make the test pass.
// Right now this returns a wrong value on purpose (that's the RED state).
// Replace the body so it actually adds x and y, then run the test (GREEN).
func Add(x, y int) int {
	return 0 // TODO(ben): make this correct
}

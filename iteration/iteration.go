//go:build !solution

package iteration

// Repeat returns s written out count times in a row.
//
// MODULE 2 — ITERATION.  Make the test pass using a `for` loop.
// Right now it returns the wrong thing (that's the RED state).
// Build up the result one copy at a time, then run the test (GREEN).
func Repeat(s string, count int) string {
	return "" // TODO(you): loop count times, adding s each pass
}

{
  "name": "gym-app",
  "version": "0.1.0",
  "private": true,
  "description": "The Go Gym GUI — local web app with a background Claude tutor",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"node server/index.mjs\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm run build && node server/index.mjs",
    "spike": "node spike.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.167",
    "express": "^5.2.1",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
